---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "crowci_organization_registry Resource - terraform-provider-crowci"
subcategory: ""
description: |-
  Manages a container registry credential scoped to an organization on the Crow CI server.
---

# crowci_organization_registry (Resource)

Manages a container registry credential scoped to an organization on the Crow CI server.

## Example Usage

```terraform
resource "crowci_organization_registry" "example" {
  org_id   = 1
  address  = "docker.io"
  username = "myuser"
  password = "mypassword"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `address` (String) Registry address (e.g. 'docker.io', 'ghcr.io').
- `org_id` (Number) ID of the organization this registry belongs to.
- `password` (String, Sensitive) Registry password. Not returned by the API after creation.
- `username` (String) Registry username.

### Read-Only

- `created_at` (Number) Creation time as a Unix timestamp.
- `id` (Number) Registry ID assigned by Crow CI.
- `readonly` (Boolean) Whether the registry is read-only.
- `repo_id` (Number) Repo scope of the registry.
- `updated_at` (Number) Last update time as a Unix timestamp.

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = crowci_organization_registry.example
  identity = {
    org_id  = 1
    address = "docker.io"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `address` (String) Registry address.
- `org_id` (Number) ID of the organization this registry belongs to.

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = crowci_organization_registry.example
  id = "1/docker.io"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import crowci_organization_registry.example 1/docker.io
```
